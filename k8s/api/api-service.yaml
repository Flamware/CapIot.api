apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: capiot-app
spec:
  selector:
    app: api
  ports:
    - protocol: TCP
      port: 8080 # Service port
      targetPort: 8080 # Pod port
  type: ClusterIP