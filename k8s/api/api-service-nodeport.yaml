apiVersion: v1
kind: Service
metadata:
  name: api-service-nodeport
  namespace: capiot-app
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 31000 # Change this line to a different available port
  selector:
    app: api